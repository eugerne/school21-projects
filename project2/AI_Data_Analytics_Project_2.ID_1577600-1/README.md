# Знакомство с NumPy
В рамках проекта ты освоишь основы работы с библиотекой NumPy, научишься выполнять операции с многомерными массивами и использовать векторизованные вычисления. Кроме того, ты закрепишь навыки тестирования и применишь NumPy для решения прикладных задач.

## Оглавление
1. [Глава I](#глава-i) \
    1.1. [Преамбула](#преамбула)
2. [Глава II](#глава-ii) \
    2.1. [Общая инструкция](#общая-инструкция)
3. [Глава III](#глава-iii) \
    3.1. [Цели](#цели) 
4. [Глава IV](#глава-iv) \
    4.1. [Задание](#задание)
5. [Глава V](#глава-v) \
    5.1. [Сдача работы и проверка](#сдача-работы-и-проверка)

## Глава I
### Преамбула
Библиотека NumPy занимает центральное место среди инструментов для работы с данными в Python. Она позволяет удобно и быстро работать с многомерными массивами и матрицами, поддерживает широкий набор математических функций и методов линейной алгебры. В отличие от стандартных структур данных Python, NumPy дает возможность выполнять векторизованные операции, что значительно ускоряет вычисления и делает обработку больших массивов информации реально практичной. На NumPy опираются многие популярные библиотеки — от Pandas и SciPy до Scikit-Learn, поэтому ее изучение становится необходимым шагом для любого специалиста в области анализа данных.

Помимо освоения инструментов анализа данных, важной частью практики DS-специалиста остается написание тестов. Они помогают убедиться, что решения работают корректно, и повышают надежность кода в реальных проектах. 

## Глава II
### Общая инструкция
Методология «Школы 21» может быть не похожа на тот образовательный опыт, который случался с тобой ранее. Ее отличает высокий уровень автономии: у тебя есть задача, ты должен ее выполнить. По большей части тебе нужно будет самому добывать знания для ее решения. Второй важный момент — это peer-to-peer обучение. В образовательном процессе нет менторов и экспертов, перед которыми ты защищаешь свой результат. Ты это делаешь перед такими же учащимися, как и ты сам. У них есть чек-лист, который поможет им качественно выполнить приемку твоей работы.

Роль «Школы 21» заключается в том, чтобы обеспечить через последовательность заданий и оптимальный уровень поддержки такую траекторию обучения, при которой ты не только освоишь hard skills, но и научишься самообучаться.
- Не доверяй слухам и предположениям о том, как должно быть оформлено твое решение. Этот документ является единственным источником, к которому стоит обращаться по большинству вопросов.
- Тебе необходимо презентовать свое решение другим учащимся и получить от них обратную связь.
- Подлежат обсуждению только те файлы, которые ты сдал в качестве решения.
- После выполнения задания загружай только те файлы, что были указаны в задании.
- Пользуйся всеми доступными средствами поиска информации, к примеру, Google и GigaChat. Будь внимателен к источникам информации: проверяй, думай, анализируй, сравнивай.
- Не стесняйся просить помощи: вокруг тебя такие же пиры, которые тоже проходят этот путь впервые. Не бойся откликаться на просьбы о помощи. Твой опыт ценен и полезен, смело делись им с другими участниками. Обсуждение заданий можно вести в мессенджерах.
- Будь внимателен к примерам, указанным в этом документе, — они могут иметь важные детали, которые не были оговорены другим способом.
- Не списывай, а если пользуешься помощью — всегда разбирайся до конца, почему, как и зачем. Иначе твое обучение не будет иметь никакого смысла.
- Если ты на чем-то застрял и кажется, что все уже перепробовал, но по-прежнему непонятно, куда идти, — просто передохни! Поверь, этот совет помогал многим разработчикам в их работе. Проветрись, перезагрузи голову, и, возможно, в следующий раз тебе наконец придет нужное решение.
- Важен не только результат обучения, но и сам процесс. Нужно не просто решить задачу, а понять, КАК ее решить.
- И да пребудет с тобой Сила!

Как работать с проектом: 
- Вся работа выполняется на виртуальной машине. Для начала ее нужно настроить, воспользовавшись [инструкцией](https://applicant.21-school.ru/guide_vm_rudn). Далее запустить виртуальную машину и продолжать выполнять работу над проектом там.
- Перед выполнением проект необходимо склонировать с GitLab в одноименный репозиторий.
- Все файлы с кодом необходимо создавать в папке src склонированного репозитория.
- После клонирования проекта необходимо создать ветку `develop` и вести разработку в ней. После этого пушить в GitLab также нужно ветку `develop`.
- В файле `reading list` папки `materials` ты найдешь полезные ресурсы для дальнейшего погружения в тему проекта.

## Глава III
### Цели
В этом проекте мы предлагаем тебе познакомиться с библиотекой **NumPy** и на практике убедиться в ее удобстве для работы с массивами и матрицами. Ты узнаешь, как с помощью NumPy можно выполнять быстрые и наглядные операции, которые в обычном Python потребовали бы гораздо больше кода и времени.

Чтобы закрепить материал, тебе предстоит реализовать несколько небольших функций на чистом **Python** и с использованием **NumPy**, чтобы почувствовать разницу в скорости вычислений. Для этого ты создашь **декоратор** для 
измерения времени выполнения функций и сравнишь производительность обоих подходов.

Далее ты воспользуешься **математическими функциями NumPy** для решения задач линейной регрессии и **сжатия изображений**, чтобы увидеть, насколько библиотека удобна для работы с числовыми данными и обработкой матриц.

В завершение ты решишь прикладную задачу — создашь **решатель головоломок судоку**, где через работу с игровым полем убедишься, насколько просто и эффективно можно описывать преобразования данных, используя NumPy. Кроме того, ты научишься **писать тесты** и поймешь их роль в проверке корректности решений. Этот навык позволит тебе
убедиться, что реализованные алгоритмы работают правильно, а также сформирует хорошую привычку к контролю качества кода.

В качестве бонусного (необязательного) задания ты реализуешь классическую игру **«Жизнь»**, чтобы увидеть, как NumPy позволяет элегантно описывать клеточные автоматы и другие вычислительные модели.

## Глава IV
### Подготовка окружения
#### Задание 0.1. Установка библиотек
Для начала подготовь рабочее окружение проекта:
1. Создай виртуальное окружение с именем `project2`. 
2. Установи необходимые библиотеки из файла [`requirements.txt`](requirements.txt).
3. Активируй виртуальное окружение.
4. Выведи список пакетов, **не являющихся зависимостями** других установленных библиотек (используй команду `pip list --not-required`). 
5. Сделай **скриншот терминала**, на котором видно:
   * активированное виртуальное окружение, 
   * вывод списка установленных пакетов.
6. Сохрани скриншот в папке [assets](assets).

### Сравнение скорости Python и NumPy

В первом задании тебе предстоит на практике сравнить скорость выполнения операций в чистом Python и с использованием NumPy. 

#### Декораторы
**Декораторы** — это специальный механизм в Python, который позволяет дополнять поведение функции без изменения ее кода. Они «оборачивают» функцию в другую, добавляя нужный функционал — например, логирование, проверку прав, кеширование или измерение времени выполнения.

Синтаксически декоратор — это просто функция, которая принимает другую функцию и возвращает новую, измененную. Обычно декораторы применяются с помощью символа @ над определением функции, например:
```python
@decorator_name
def my_function():
    ...
```
Мы хотим **сравнить время выполнения функций**, реализованных на чистом Python и с использованием NumPy. 
Конечно, можно было бы вставить код замера времени прямо внутри каждой функции, но в этом случае код стал бы громоздким и повторяющимся, а логика замера времени смешалась бы с основной задачей.

Чтобы сделать код **более аккуратным, читаемым и удобным для повторного использования**, мы воспользуемся **декоратором**.  

#### Задание 1.1. Замер скорости
1. Реализуй декоратор `@benchmark`, который:
    * принимает функцию,
    * измеряет время ее выполнения,
    * выводит результат в **секундах**.  
2. Протестируй работу декоратора на функции `python_mean`, которая вычисляет **среднее арифметическое** элементов списка на чистом Python.
3. В качестве теста передай в функцию список из **1 000 000 элементов**.

#### Задание 1.2. Базовые функции
1. Реализуй следующие функции:
    - **Функция 1:** вычисление среднего арифметического элементов массива.
    - **Функция 2:** создание квадратной матрицы **n × n**, заполненной случайными числами.
    - **Функция 3:** проверка, есть ли в матрице **нулевые столбцы** (столбцы, где все элементы равны нулю).
    - **Функция 4:** вычисление **суммы элементов на главной диагонали** матрицы. 
2. Для каждой функции создай **два варианта реализации**:
    - на **чистом Python**,
    - с использованием **NumPy**.
3. Покажи, что **обе версии дают одинаковый результат** на одинаковых входных данных.

#### Задание 1.3. Сравнение производительности Python и NumPy
Теперь, когда у тебя есть реализации функций и декоратор для измерения времени, сравни производительность 
Python и NumPy на практике:
1. Для каждой функции сравни время выполнения ее реализации на чистом Python и с использованием NumPy с помощью декоратора `@benchmark`.
2. Выведи результаты в удобном виде (например, в виде таблицы или форматированного текста).
3. Сделай вывод о различиях в скорости работы и объясни, за счет чего NumPy работает быстрее. 

### Головоломка судоку
**NumPy** — это мощный инструмент для работы с массивами и матрицами. Чтобы почувствовать себя увереннее при работе с матрицами, мы попробуем реализовать **решатель головоломок судоку**.

#### Задание 2.1. Запрос по API
* Изучи API [YouDoSudoku](https://www.youdosudoku.com/) и разберись, как получать игры судоку и их решения.
* Напиши функцию `get_sudoku(level)`, которая принимает уровень сложности (`'easy'`, `'medium'`, `'hard'`).
* Функция должна возвращать две матрицы `numpy` размером 9×9: одну — с головоломкой, другую — с полным решением.
* После запроса выведи обе матрицы, чтобы убедиться, что данные получены и преобразованы правильно.

#### Задание 2.2. Написание тестов
* Используя библиотеку `ipytest`, напиши тесты, которые проверяют корректность работы решения судоку, чтобы убедиться, что решатель работает правильно.
* Каждый тест должен содержать [docstring](https://peps.python.org/pep-0257/), в котором кратко описано, какое правило судоку он проверяет.
* Один тест должен проверять одно правило судоку.

#### Задание 2.3. Реализация алгоритма решения
* Напиши алгоритм решения судоку. Можно использовать **brute-force (backtracking)**. Алгоритм должен заполнять все пустые клетки числами от 1 до 9, соблюдая правила судоку для строк, столбцов и 3×3 блоков.
* Проверь работу алгоритма с помощью ранее написанных тестов и убедись, что они покрывают все крайние случаи.
* Получи через [YouDoSudoku API](https://www.youdosudoku.com/) по 5 примеров судоку разной сложности, реши их своим алгоритмом и выведи на экран как исходные поля, так и решения. Проверь, что решения корректны.

## Сингулярное разложение (SVD)
В библиотеке NumPy реализовано множество полезных математических функций: операции с массивами, линейная алгебра, вычисление статистических показателей, тригонометрические и экспоненциальные функции, преобразования Фурье, 
решение систем уравнений, а также **сингулярное разложение матриц**.

Любую матрицу (A) можно разложить следующим образом:

$$A = U \times S \times V^T$$

Где:
* $U$ — матрица **левых сингулярных векторов**,
* $S$ — **диагональная матрица сингулярных чисел**,
* $V^T$ — матрица **правых сингулярных векторов** (транспонированная).

Это разложение называется **сингулярное разложение**, и оно широко используется в **анализе данных** и **машинном обучении**:

* **сжатие изображений** и сигналов;
* **рекомендательные системы** (например, в коллаборативной фильтрации);
* **выделение основных компонентов** (PCA);
* **аппроксимация матриц** и уменьшение размерности;
* в задачах **анализ текста** (Latent Semantic Analysis).

Матрица $S$ содержит **сингулярные числа**, которые несут основную информацию о матрице (например, изображении).

Если сохранить только **k наибольших сингулярных чисел**, мы можем получить приближенную матрицу:

$$A_k = U[:, :k] \times S[:k, :k] \times V^T[:k, :]$$

* Это позволяет **сжимать изображения**, сохраняя ключевую информацию и убирая шум.
* Чем меньше (k), тем сильнее сжатие, но при этом может теряться часть деталей.

В следующих заданиях мы воспользуемся SVD для сжатия изображений, чтобы на практике увидеть, как сохраняются важные признаки картинки при уменьшении ее размерности.

### Задание 3.1. Подготовка изображения и SVD-разложение
1. С помощью библиотеки `Pillow` загрузи изображение [jackal.png](misc/jackal.png).
<center><img src="misc/jackal.png" height="150" width="150"></center>

2. Преобразуй изображение в `np.array`.
3. Примени **SVD-разложение** к массиву с помощью `np.linalg.svd`.
4. Выведи размерность изображения и матриц $U$, $S$, $V$.

### Задание 3.2. Сжатие изображения и визуализация результатов
1. Определи набор значений (k) — количество сингулярных чисел для сохранения. Рекомендуемые значения: [100, 50, 25, 20, 10].
2. Для каждого (k) создай **приближенную версию изображения**:
   $$A_k = U[:, :k] \times S[:k, :k] \times V^T[:k, :]$$
3. Визуализируй результаты:
   * Размести **оригинальное изображение** в первом subplot'е.
   * Размести все сжатые версии в остальных subplot'ах.
   * Добавь **заголовки**, указывающие значение (k).

<center><img src="misc/jackals.png" height="200" width="300"></center>

### Бонусное задание. Игра «Жизнь» (Conway’s Game of Life)
**«Жизнь»** — это игра, придуманная математиком Джоном Конвеем в 1970 году. Игра проходит на двумерной сетке клеток, где каждая клетка может быть **живой** или **мертвой**.

### Основные правила:
1. **Выживание:**   
   - Живая клетка с **2 или 3 живыми соседями** остается живой.
2. **Смерть:**   
   - **от одиночества:** живая клетка с **менее чем 2 живыми соседями** умирает;
   - **от перенаселения:** живая клетка с **более чем 3 живыми соседями** умирает.
3. **Рождение:**   
   - Мертвая клетка с **ровно 3 живыми соседями** становится живой.

![game_of_life](misc/images/game_of_life.gif)

## Задание 4.1. Реализация игры «Жизнь» 

В этом задании тебе нужно реализовать класс `GameOfLife` в файле [`game.py`](src/game.py), который моделирует эволюцию клеток по правилам игры. Задание не является обязательным для выполнения и не влияет на успешность выполнения проекта. 

Для этого:
1. Реализуй метод **`count_neighbors(self, grid)`**. Он должен:
   * подсчитывать количество живых соседей для каждой клетки;
   * не учитывать саму клетку при подсчете.
2. Реализуй метод **`update(self)`**. Он должен:
   * получать количество соседей для каждой клетки;
   * применять правила игры:
     * живая клетка с 2 или 3 соседями выживает;
     * мертвая клетка с ровно 3 соседями становится живой;
     * остальные клетки умирают или остаются мертвыми;
   * использовать **булевы операции NumPy** для эффективности.
3. С помощью `matplotlib.animation.FuncAnimation` визуализируй эволюцию игры:
   * сетка размера `size x size`;
   * количество шагов: `frames`.
4. Сохрани полученную анимацию в формате **GIF** в папку `assets`.

## Глава V
### Сдача работы и проверка
Сохрани решение в файле `02-assignment.ipynb` и `game.py`. \
Загрузи изменения на Git в ветку develop.

___

💡 [Нажми сюда](http://opros.so/p7AyT), **чтобы отправить обратную связь по проекту**. 

